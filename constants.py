from pygame.colordict import THECOLORS as colors

# colors
BACKGROUND_COLOR = colors.get("black")

# Ball
BALL_RADIUS = 15

# Screen
SCREEN_WIDTH = 1280
SCREEN_HEIGHT = 800

# Field
FIELD_WIDTH = SCREEN_WIDTH
FIELD_HEIGHT = 720

# Status bar
STATUS_HEIGHT = SCREEN_HEIGHT - FIELD_HEIGHT

# Scoreboard
SCOREBOARD_HEIGHT = round(1.5 * STATUS_HEIGHT)
BOARD_IMG_RESOLUTION = (5064, 2422)
SCALE_RATIO = SCOREBOARD_HEIGHT / BOARD_IMG_RESOLUTION[1]
## Clock
CLOCK_ORIGINAL_POS = (1870, 1230)
CLOCK_POS = (
    CLOCK_ORIGINAL_POS[0] * SCALE_RATIO,
    CLOCK_ORIGINAL_POS[1] * SCALE_RATIO,
)
#print(CLOCK_POS)
CLOCK_FRAME_HEIGHT = 35
DIGIT_WIDTH = round(CLOCK_FRAME_HEIGHT * 0.42)
CLOCK_CENTER = (
    CLOCK_POS[0] + 2.25 * DIGIT_WIDTH,
    CLOCK_POS[1] + CLOCK_FRAME_HEIGHT / 2,
)
CLOCK_DIGIT_POSITIONS = [
    CLOCK_POS,
    (CLOCK_POS[0] + DIGIT_WIDTH, CLOCK_POS[1]),
    (CLOCK_POS[0] + DIGIT_WIDTH * 2.5, CLOCK_POS[1]),
    (CLOCK_POS[0] + DIGIT_WIDTH * 3.5, CLOCK_POS[1]),
]
## Scores
ALLY_SCORE_ORIG_X = 260
OPPONENT_SCORE_ORIG_X = 3470
SCORES_ORIG_Y = 690
SCORE_ORIG_SIZE = (1370, 1400)
SCORES_CENTERS = {
    "ally": (
        (ALLY_SCORE_ORIG_X + SCORE_ORIG_SIZE[0] / 2) * SCALE_RATIO,
        (SCORES_ORIG_Y + SCORE_ORIG_SIZE[1] / 2) * SCALE_RATIO,
    ),
    "opponent": (
        (OPPONENT_SCORE_ORIG_X + SCORE_ORIG_SIZE[0] / 2) * SCALE_RATIO,
        (SCORES_ORIG_Y + SCORE_ORIG_SIZE[1] / 2) * SCALE_RATIO,
    ),
}

#player
PLAYER_SPIN_COUNTDOWN = 100
